import 'dart:io';

List<int> getFib(int n) {
  List<int> fib = [0, 1];

  for (int i = 2; i < n; i++) {
    fib.add(fib[i - 1] + fib[i - 2]);
  }

  return fib.take(n).toList();
}

void main() {
  print("How many numbers?");
  int n = int.parse(stdin.readLineSync()!);

  List<int> result = getFib(n);

  print(result);
}
